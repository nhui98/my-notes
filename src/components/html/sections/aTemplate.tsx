import SectionTitle from "../SectionTitle";

const Template = () => (
  <section className="html-section">
    <SectionTitle title="" />
    <div className="html-section-layout">{/* tags */}</div>
  </section>
);

export default Template;
